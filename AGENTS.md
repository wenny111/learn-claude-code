# Codex 工作规范

## 代码注释

- 在本项目中添加或修改实现注释时，优先使用简短的中文 `#` 注释。
- 不使用三引号字符串充当说明性注释，注释风格统一为 `#`。
- 只注释核心实现和关键位置，不重复解释一眼可见的代码。
- 面向 Python 阅读经验较少的读者，说明关键数据结构和算法，例如 `list[dict]`、元组返回、滑动窗口、反向扫描、编号映射、降级匹配、浅拷贝和批量替换。
- 注释应说明具体实现“在做什么、为什么这样做”，并紧贴相关代码。
- 不为了添加注释而改变程序行为。
